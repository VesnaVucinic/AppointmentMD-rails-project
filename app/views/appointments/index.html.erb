<h1> Listing appointments <h1>

  <table>
    <tr>
      <th>Physician</th>
      <th>Patient</th>
      <th></th>
      <th></th>
      <th></th>
     </tr>


     <% @appointments.each do |appointment| %>
     <tr>
       <td><%= link_to Physician.find(appointment.physician_id).name, appointment.physician %></td>
       <td><%= link_to Patient.find(appointment.patient_id).name, appointment.patient %></td>
       <td><%= link_to Location.find(appointment.location_id)name, appointment.loaction %></td>
       <td><%= link_to 'Show', appointment %></td>
       <td><%= link_to 'Edit', edit_appointment_path(appointment) %></td>
       </tr>
       <% end %>
       </table>
       <br>


       <%= link_to 'Create a new appointment', new_appointment_path %>
